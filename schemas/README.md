# Schemas

This directory is reserved for versioned schemas that describe Pivot's future
configuration formats. Slice 0 defines no manifest or registry schema. A schema
will be introduced with the implementation it validates and reviewed for strict
validation, compatibility, and safe defaults.
